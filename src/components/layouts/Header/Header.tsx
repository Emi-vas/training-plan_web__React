import React from 'react';
//styles
import { Wrapper } from './Header.styles';

const Header = () => {
    return (
        <Wrapper>
            Header
        </Wrapper>
    );
};

export default Header;