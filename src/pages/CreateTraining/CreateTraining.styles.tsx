import styled from "styled-components";

interface PropsTypeTraining {
    image: string
}

export const ChooseTypeWrapper = styled.div`
    display: flex;
    justify-content: center;
    flex-wrap: wrap;
`